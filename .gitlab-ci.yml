include:
  - project: quantum/cicd/templates/python
    file: quantum-toolbox-template-microlib.yml
    ref: "python312"


python:twine:
  rules:
    - when: never

semantic-release:
  rules:
    - when: never
